name: Apply Analysis Simple Advanced mode

on:
  push:
    branches:
      - feature/analysis-simple-advanced-mode

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: feature/analysis-simple-advanced-mode

      - name: Apply targeted patch
        run: node scripts/apply-analysis-simple-advanced-mode.mjs

      - name: Commit integration
        run: |
          if git diff --quiet; then
            echo "No integration changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/AnalysisCenter.tsx src/styles/analysis-panel-professional.css src/test/analysis.smoke.test.tsx
          git commit -m "add simple and advanced Analysis UI modes"
          git push
